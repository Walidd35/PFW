---
import data from '../data/projects.json';
const { profile } = data;
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-text">
        <h3>Restons en contact</h3>
        <p>Toujours intéressé par de nouveaux projets et collaborations.</p>
      </div>
      
      <div class="footer-links">
        <a href={profile.linkedin} target="_blank" rel="noopener noreferrer" class="footer-link">
          💼 LinkedIn
        </a>
        
        <a href={profile.github} target="_blank" rel="noopener noreferrer" class="footer-link">
          🔗 GitHub
        </a>
        
        <a href={`mailto:${profile.email}`} class="footer-link">
          ✉️ Email
        </a>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} {profile.name}. Réalisé avec ❤️ et AstroJS</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
    color: white;
    padding: 3rem 0 1rem;
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
    margin-bottom: 2rem;
  }
  
  .footer-text h3 {
    color: white;
    margin-bottom: 0.5rem;
  }
  
  .footer-text p {
    opacity: 0.8;
    line-height: 1.6;
  }
  
  .footer-links {
    display: flex;
    gap: 1.5rem;
    justify-content: flex-end;
  }
  
  .footer-link {
    color: white;
    text-decoration: none;
    padding: 0.75rem 1rem;
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
    font-weight: 500;
  }
  
  .footer-link:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
  }
  
  .footer-bottom {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    opacity: 0.7;
  }
  
  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .footer-links {
      justify-content: center;
      flex-wrap: wrap;
    }
  }
</style>